# Frontend (Next.js)

## Setup
```
npm install
copy .env.local.example .env.local   (Windows)
cp .env.local.example .env.local     (Mac/Linux)
```

## Run
```
npm run dev
```
Visit http://localhost:3000 -> should show "Hello World — frontend is running."